
# Programa que cuenta desde 1 hasta el número introducido por el usuario

# Solicitamos al usuario que ingrese un número
numero = int(input("Ingresa el número: "))

# Inicializamos el contador en 1
contador = 1

# Utilizamos un bucle while para mostrar los números desde 1 hasta el número ingresado
while contador <= numero:
    print(contador)
    # Aumentamos el contador para evitar un bucle infinito
    contador += 1
</antArtifact>

Este programa hace exactamente lo que solicitaste:

1. Solicita al usuario que ingrese un número
2. Inicializa una variable contador en 1
3. Utiliza un bucle while que se ejecuta mientras el contador sea [REDACTED_INFRASTRUCTURE] o igual al número ingresado
4. En cada iteración, muestra el valor actual del contador
5. Incrementa el contador en cada iteración para evitar un bucle infinito

Si el usuario ingresa 7, la salida será exactamente como la que has indicado:
```
Ingresa el número: 7
1
2
3
4
5
6
7
```

¡Espero que este código te sea útil!